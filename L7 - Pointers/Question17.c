// Write a function using pointers to replace all occurrences of a character in a string with another character.
#include <stdio.h>

void replaceChar(char *str, char find, char replace) {
    // Your code here
}

int main() {
    char str[] = "Hello World";
    // Your code here to call replaceChar and print the result
    return 0;
}