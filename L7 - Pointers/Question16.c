// Implement a function to copy an array of integers to another array using pointers.
#include <stdio.h>

void copyArray(int *source, int *destination, int n) {
    // Your code here
}

int main() {
    int source[] = {1, 2, 3, 4, 5};
    int destination[5];
    int n = sizeof(source) / sizeof(source[0]);
    // Your code here to call copyArray and print the destination array
    return 0;
}