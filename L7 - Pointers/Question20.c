// Implement a function to perform a binary search on a sorted array of integers using pointers. Return the index of the found element or -1 if not found.
#include <stdio.h>

int binarySearch(int *arr, int n, int target) {
    // Your code here
}

int main() {
    int arr[] = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    int n = sizeof(arr) / sizeof(arr[0]);
    int target = 4;
    // Your code here to call binarySearch and print the result
    return 0;
}