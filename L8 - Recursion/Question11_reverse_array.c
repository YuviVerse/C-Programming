#include <stdio.h>

int reverse_array(int a);

int main() {
    int array[] = {1, 2, 3, 4, 5}; int size = 5;
    printf("Reverse an Array: %d\n", reverse_array(a));
    return 0;
}

// Question 11: Reverse an array using recursion.
// Students to implement the reverse_array function
int reverse_array(int a) {
    // Implementation needed
}
