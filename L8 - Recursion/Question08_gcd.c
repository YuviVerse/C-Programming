#include <stdio.h>

int gcd(int a);

int main() {
    int a = 36, b = 60;
    printf("Greatest Common Divisor: %d\n", gcd(a));
    return 0;
}

// Question 8: Find the greatest common divisor (GCD) of two numbers using recursion.
// Students to implement the gcd function
int gcd(int a) {
    // Implementation needed
}
