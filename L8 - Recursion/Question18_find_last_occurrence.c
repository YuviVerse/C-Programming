#include <stdio.h>

int find_last_occurrence(int a);

int main() {
    int array[] = {1, 2, 3, 4, 2, 5}; int size = 6; int target = 2;
    printf("Find the Last Occurrence of a Number in an Array: %d\n", find_last_occurrence(a));
    return 0;
}

// Question 18: Find the last occurrence of a given number in an array.
// Students to implement the find_last_occurrence function
int find_last_occurrence(int a) {
    // Implementation needed
}
