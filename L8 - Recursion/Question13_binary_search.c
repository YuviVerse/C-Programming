#include <stdio.h>

int binary_search(int a);

int main() {
    int array[] = {10, 20, 30, 40, 50, 60, 70}; int size = 7; int target = 40;
    printf("Binary Search: %d\n", binary_search(a));
    return 0;
}

// Question 13: Perform a binary search on a sorted array.
// Students to implement the binary_search function
int binary_search(int a) {
    // Implementation needed
}
