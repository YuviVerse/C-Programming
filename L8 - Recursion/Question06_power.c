#include <stdio.h>

int power(int a);

int main() {
    int base = 2, exponent = 8;
    printf("Power of a Number: %d\n", power(a));
    return 0;
}

// Question 6: Compute x raised to the power of n.
// Students to implement the power function
int power(int a) {
    // Implementation needed
}
