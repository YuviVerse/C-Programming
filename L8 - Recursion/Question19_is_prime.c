#include <stdio.h>

int is_prime(int a);

int main() {
    int number = 29;
    printf("Checking Prime Number: %d\n", is_prime(a));
    return 0;
}

// Question 19: Check if a number is prime using recursion.
// Students to implement the is_prime function
int is_prime(int a) {
    // Implementation needed
}
