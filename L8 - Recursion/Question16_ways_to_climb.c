#include <stdio.h>

int ways_to_climb(int a);

int main() {
    int steps = 10;
    printf("Number of Ways to Climb Stairs: %d\n", ways_to_climb(a));
    return 0;
}

// Question 16: Find the number of ways to climb n stairs if you can climb 1 or 2 steps at a time.
// Students to implement the ways_to_climb function
int ways_to_climb(int a) {
    // Implementation needed
}
