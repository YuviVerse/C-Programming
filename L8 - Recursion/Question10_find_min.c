#include <stdio.h>

int find_min(int a);

int main() {
    int array[] = {7, 2, 8, 6, 4}; int size = 5;
    printf("Find Minimum in an Array: %d\n", find_min(a));
    return 0;
}

// Question 10: Find the minimum element in an array.
// Students to implement the find_min function
int find_min(int a) {
    // Implementation needed
}
