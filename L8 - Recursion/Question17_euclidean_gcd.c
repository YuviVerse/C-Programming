#include <stdio.h>

int euclidean_gcd(int a);

int main() {
    int a = 1071, b = 462;
    printf("Euclidean Algorithm for GCD: %d\n", euclidean_gcd(a));
    return 0;
}

// Question 17: Implement the Euclidean algorithm recursively to find the GCD of two numbers.
// Students to implement the euclidean_gcd function
int euclidean_gcd(int a) {
    // Implementation needed
}
