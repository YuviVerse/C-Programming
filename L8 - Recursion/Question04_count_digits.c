#include <stdio.h>

int count_digits(int a);

int main() {
    int number = 12345;
    printf("Counting Digits in a Number: %d\n", count_digits(a));
    return 0;
}

// Question 4: Count the number of digits in a given integer.
// Students to implement the count_digits function
int count_digits(int a) {
    // Implementation needed
}
