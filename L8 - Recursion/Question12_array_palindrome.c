#include <stdio.h>

int array_palindrome(int a);

int main() {
    int array[] = {1, 2, 3, 2, 1}; int size = 5;
    printf("Array Palindrome Check: %d\n", array_palindrome(a));
    return 0;
}

// Question 12: Check if an array is a palindrome.
// Students to implement the array_palindrome function
int array_palindrome(int a) {
    // Implementation needed
}
