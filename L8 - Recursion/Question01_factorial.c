#include <stdio.h>

int factorial(int a);

int main() {
    int number = 5;
    printf("Factorial Calculation: %d\n", factorial(a));
    return 0;
}

// Question 1: Calculate the factorial of a given number.
// Students to implement the factorial function
int factorial(int a) {
    // Implementation needed
}
