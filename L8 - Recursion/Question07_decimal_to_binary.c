#include <stdio.h>

int decimal_to_binary(int a);

int main() {
    int number = 31;
    printf("Decimal to Binary Conversion: %d\n", decimal_to_binary(a));
    return 0;
}

// Question 7: Converts a decimal number to binary.
// Students to implement the decimal_to_binary function
int decimal_to_binary(int a) {
    // Implementation needed
}
