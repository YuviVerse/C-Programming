// Write a function that counts the number of digits in a given integer. What is the digit count of 12345?
#include <stdio.h>

int countDigits(int number) {
    // Your code here
}

int main() {
    int count = countDigits(12345);
    printf("Number of digits in 12345: %d\n", count);
    return 0;
}