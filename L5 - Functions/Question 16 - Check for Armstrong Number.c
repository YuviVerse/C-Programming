// Implement a function to check if a 3-digit number is an Armstrong number. 
// Use it to check number 153.
#include <stdio.h>

int isArmstrong(int number) {
    // Your code here
}

int main() {
    int number = 153;
    if (isArmstrong(number)) {
        printf("%d is an Armstrong number.\n", number);
    } else {
        printf("%d is not an Armstrong number.\n", number);
    }
    return 0;
}
