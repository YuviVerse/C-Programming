// Implement a function to check if a number is prime and print the result for n = 29.
#include <stdio.h>

int isPrime(int n) {
    // Your code here
}

int main() {
    int number = 29;
    if (isPrime(number)) {
        printf("%d is a prime number.\n", number);
    } else {
        printf("%d is not a prime number.\n", number);
    }
    return 0;
}