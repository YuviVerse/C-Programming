// Implement a function that checks if a number is prime. The function should return 1 if the number is prime and 0 otherwise.
#include <stdio.h>

int isPrime(int number) {
    // Your code here
}

int main() {
    int number = 29;
    if (isPrime(number)) {
        printf("%d is a prime number.\n", number);
    } else {
        printf("%d is not a prime number.\n", number);
    }
    return 0;
}
