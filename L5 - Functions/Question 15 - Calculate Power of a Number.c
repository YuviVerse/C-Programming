// Implement a function that calculates and returns the value of x raised to 
// the power of y (x^y). Assume y is a non-negative integer.
#include <stdio.h>

int power(int x, int y) {
    // Your code here
}

int main() {
    int base = 2, exponent = 3;
    printf("%d raised to the power of %d is %d\n", base, exponent, power(base, exponent));
    return 0;
}