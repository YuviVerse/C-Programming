// Implement a function that mimics the behavior of the strstr function, which finds the first occurrence of a substring in a string. If found, return the index; otherwise, return -1.
#include <stdio.h>

int customStrstr(char str[], char substr[]) {
    // Your code here
}

int main() {
    char str[] = "This is a simple string";
    char substr[] = "simple";
    // Your code here to call customStrstr and print the result
    return 0;
}
