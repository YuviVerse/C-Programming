// Implement a function that mimics the behavior of the atoi function, which converts a string to an integer. The function should handle leading whitespaces and optional leading '+' or '-' sign.
#include <stdio.h>

int customAtoi(char str[]) {
    // Your code here
}

int main() {
    char str[] = "   -123";
    // Your code here to call customAtoi and print the result
    return 0;
}
