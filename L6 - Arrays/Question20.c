// Given a string containing just the characters '(', ')', '{', '}', '[' and ']', write a function to check if the input string is valid. An input string is valid if:
// 1. Open brackets must be closed by the same type of brackets.
// 2. Open brackets must be closed in the correct order.
#include <stdio.h>

int isValid(char* s) {
    // Your code here
}

int main() {
    char str[] = "{[]}";
    // Your code here to call isValid and print the result
    return 0;
}
