// Write a function to encode a string by shifting each letter by 3 positions in the alphabet. E.g., 'a' becomes 'd', 'z' becomes 'c'. Assume lowercase input.
#include <stdio.h>

void encodeString(char str[]) {
    // Your code here
}

int main() {
    char str[] = "abcxyz";
    // Your code here to call encodeString and print the result
    return 0;
}
