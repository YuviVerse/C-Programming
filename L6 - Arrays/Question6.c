// Check if the array contains any duplicate elements. Print "Duplicates found" or "No duplicates".
#include <stdio.h>

int main() {
    int elements[] = {1, 2, 3, 4, 5, 1};
    // Your code here
    return 0;
}