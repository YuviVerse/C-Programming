//Write a program that asks for two integers, a and b, and prints "a is greater",
//"b is greater", or "a and b are equal", depending on their values.

#include <stdio.h>

int main() {
    int a, b;
    printf("Enter two integers (a and b): ");
    scanf("%d %d", &a, &b);
    
    // Your code here
    
    return 0;
}